interface:
  display_name: "Requisite Catalog Import"
  short_description: "Maintain catalog generation and DB import"
  default_prompt: "Use $requisite-catalog-import to update or verify catalog generation and PostgreSQL import behavior."
